[Unit]
Description=Split-flap WSGI service
After=network.target

[Service]
Type=simple
WorkingDirectory={{ wsgi_app_dir }}
ExecStart=/usr/bin/gunicorn3 --workers {{ wsgi_workers }} --bind {{ wsgi_bind }} wsgi:app
Restart=on-failure
User=root
Group=root

[Install]
WantedBy=multi-user.target
